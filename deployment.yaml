apiVersion: apps/v1
kind: Deployment
metadata:
  name: ner-classifier-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ner-classifier
  template:
    metadata:
      labels:
        app: ner-classifier
    spec:
      containers:
      - name: ner-classifier
        image: classifier.azurecr.io/ner_classifier:v2
        ports:
        - containerPort: 8000
        resources:
          limits:
            memory: "2Gi"
            cpu: "1"
---
apiVersion: v1
kind: Service
metadata:
  name: ner-classifier-service
spec:
  type: LoadBalancer
  selector:
    app: ner-classifier
  ports:
    - port: 80
      targetPort: 8000